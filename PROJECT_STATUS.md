# Second Brain — Полная информация о проекте

> **Последнее обновление:** 2026-01-12  
> **Статус:** ✅ Production (работает)

---

## 📋 Оглавление

1. [Обзор проекта](#-обзор-проекта)
2. [Production окружение](#-production-окружение)
3. [Архитектура](#-архитектура)
4. [Структура файлов](#-структура-файлов)
5. [Компоненты системы](#-компоненты-системы)
6. [Учётные данные](#-учётные-данные)
7. [Локальная разработка](#-локальная-разработка)
8. [Деплой и обновление](#-деплой-и-обновление)
9. [Мониторинг и отладка](#-мониторинг-и-отладка)
10. [Известные особенности](#-известные-особенности)
11. [Что сделано](#-что-сделано)
12. [Что планируется](#-что-планируется)

---

## 🎯 Обзор проекта

**Second Brain** — self-hosted система управления заметками на базе Obsidian.

### Основные возможности

| Функция | Описание |
|---------|----------|
| **Obsidian LiveSync** | Realtime синхронизация заметок между устройствами |
| **Telegram бот** | Быстрое создание заметок и поиск через Telegram |
| **REST API** | Программный доступ к заметкам |
| **Web Viewer** | Просмотр заметок в браузере |
| **Auto SSL** | Автоматические HTTPS сертификаты через Let's Encrypt |

### Технологический стек

| Компонент | Технология | Версия |
|-----------|------------|--------|
| База данных | CouchDB | 3.3.3 |
| Backend | FastAPI + Python | 3.11 |
| Telegram | python-telegram-bot | 21.0 |
| Reverse Proxy | Caddy | 2-alpine |
| Контейнеризация | Docker Compose | v5.0.1 |
| Хостинг | Timeweb Cloud | — |

---

## 🌐 Production окружение

### Сервер

| Параметр | Значение |
|----------|----------|
| **Провайдер** | Timeweb Cloud |
| **Server ID** | 6383807 |
| **Server Name** | second-brain |
| **IP адрес** | 82.147.71.198 |
| **ОС** | Ubuntu 22.04 LTS (5.15.0-164-generic) |
| **Регион** | ru-2 |
| **Статус** | ✅ Online |

### Домены и DNS

| Домен | Тип | Значение | Назначение |
|-------|-----|----------|------------|
| yakushev.me | — | — | Основной домен (Reg.ru) |
| notes.yakushev.me | A | 82.147.71.198 | CouchDB (Obsidian sync) |
| api.yakushev.me | A | 82.147.71.198 | REST API + Telegram бот |

### Контейнеры Docker

| Контейнер | Образ | Порты | Статус |
|-----------|-------|-------|--------|
| secondbrain-couchdb | couchdb:3.3.3 | 5984 (internal) | ✅ Healthy |
| secondbrain-api | second-brain-notes-api | 8000 (internal) | ✅ Healthy |
| secondbrain-caddy | caddy:2-alpine | 80, 443 | ✅ Running |

### Сеть и файрвол

- **Docker network:** secondbrain-network (bridge)
- **UFW:** активен, открыты порты 22, 80, 443/tcp

### Файловая структура на сервере

```
/root/
├── second-brain/           # Проект
│   ├── .env                # Конфигурация (секреты)
│   ├── docker-compose.yml
│   ├── api/
│   ├── couchdb/
│   │   └── data/           # Данные CouchDB (persistent)
│   ├── caddy/
│   │   ├── data/           # SSL сертификаты
│   │   └── config/
│   └── ...
└── second-brain-deploy.tar.gz  # Архив деплоя
```

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET                                 │
│                                                                  │
│  Obsidian Desktop/Mobile          Telegram         Browser       │
│         │                            │                │          │
└─────────┼────────────────────────────┼────────────────┼──────────┘
          │                            │                │
          ▼                            ▼                ▼
┌─────────────────────────────────────────────────────────────────┐
│  CADDY (secondbrain-caddy) - Reverse Proxy + Auto SSL           │
│  ├── :80  → redirect to :443                                    │
│  ├── :443 notes.yakushev.me → couchdb:5984                      │
│  └── :443 api.yakushev.me   → notes-api:8000                    │
└─────────────────────────────────────────────────────────────────┘
          │                            │
          ▼                            ▼
┌─────────────────────┐    ┌─────────────────────────────────────┐
│  CouchDB            │    │  FastAPI (secondbrain-api)          │
│  (secondbrain-      │◄───┤  ├── REST API endpoints             │
│   couchdb)          │    │  ├── Telegram Bot (polling)         │
│                     │    │  └── Web Viewer (/view)             │
│  :5984              │    │  :8000                              │
└─────────────────────┘    └─────────────────────────────────────┘
          ▲
          │ LiveSync protocol
┌─────────┴─────────┐
│  Obsidian         │
│  Self-hosted      │
│  LiveSync plugin  │
└───────────────────┘
```

### Потоки данных

1. **Obsidian → CouchDB**: Синхронизация через LiveSync плагин
2. **Telegram → API → CouchDB**: Создание заметок через бота
3. **Browser → API → CouchDB**: Просмотр через Web Viewer
4. **External → API**: REST запросы с X-API-Token

---

## 📁 Структура файлов

```
second-brain/
│
├── api/                          # Backend приложение
│   ├── main.py                   # FastAPI сервер (700 строк)
│   │   ├── Settings              # Pydantic конфигурация
│   │   ├── CouchDBClient         # Async клиент для CouchDB
│   │   ├── API endpoints         # /api/notes/*
│   │   └── Web Viewer            # /view (HTML)
│   ├── bot.py                    # Telegram бот (363 строки)
│   │   ├── Handlers              # /start, /note, /search, etc.
│   │   └── start_bot/stop_bot    # Lifecycle
│   ├── Dockerfile                # Python 3.11-slim
│   ├── requirements.txt          # Зависимости
│   ├── pytest.ini                # Конфигурация тестов
│   └── tests/                    # Unit тесты
│       ├── conftest.py           # Pytest фикстуры
│       └── test_api.py           # 15 тестов
│
├── couchdb/
│   └── config/
│       └── local.ini             # Конфигурация CouchDB
│
├── caddy/                        # Данные Caddy (в .gitignore)
│   ├── data/                     # SSL сертификаты
│   └── config/
│
├── scripts/
│   ├── setup-server.sh           # Настройка нового сервера
│   ├── init-couchdb.sh           # Инициализация БД
│   └── backup.sh                 # Бэкап данных
│
├── tools/                        # Инструменты разработки
│   ├── claude.py                 # Claude API CLI
│   ├── review.sh                 # Code review
│   ├── generate-tests.sh         # Генерация тестов
│   ├── debug.sh                  # Отладка
│   └── usage-stats.sh            # Статистика API
│
├── docs/
│   ├── CLAUDE_API_SETUP.md       # Настройка Claude API
│   └── DEPLOY.md                 # Инструкция деплоя
│
├── docker-compose.yml            # Оркестрация (3 сервиса)
├── Caddyfile                     # Конфигурация reverse proxy
├── Makefile                      # 296 строк, ~40 команд
├── .env.example                  # Шаблон переменных
├── .gitignore                    # Исключения Git
├── README.md                     # Основная документация
├── PLAN.md                       # План и roadmap
├── PROJECT_STATUS.md             # Этот файл
└── STATUS_RULES.md               # Правила обновления статуса
```

---

## 🔧 Компоненты системы

### 1. CouchDB (secondbrain-couchdb)

**Назначение:** Хранение заметок в формате, совместимом с Obsidian LiveSync.

**Конфигурация:**
- Single node mode
- CORS включен для всех origins (`origins = *`)
- CORS заголовки проксируются через Caddy
- Require authentication

**Базы данных:**
- `_users` — системная
- `_replicator` — системная
- `obsidian_notes` — основная база заметок

**Индексы:**
- `mtime-index` — для сортировки по времени
- `type-index` — для фильтрации по типу

### 2. FastAPI (secondbrain-api)

**Назначение:** REST API и Telegram бот.

**Endpoints:**

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/` | Health check (public) |
| GET | `/health` | Детальный health check (public) |
| GET | `/view` | Web Viewer (public, требует токен в UI) |
| POST | `/api/notes/quick` | Создать заметку |
| GET | `/api/notes/recent` | Последние заметки |
| GET | `/api/notes/search` | Поиск |
| GET | `/api/notes/summary` | Дневная сводка |
| GET | `/api/notes/{id}` | Получить заметку по ID |

**Авторизация:** Header `X-API-Token`

### 3. Telegram Bot

**Назначение:** Быстрое создание заметок через Telegram.

**Команды:**

| Команда | Описание |
|---------|----------|
| `/start` | Главное меню |
| `/note <текст>` | Создать заметку |
| `/recent` | Последние 10 заметок |
| `/summary` | Сводка за день |
| `/search <запрос>` | Поиск |
| `/help` | Справка |
| Любой текст | Сохранить как заметку |

**Безопасность:** Бот отвечает только пользователю с TELEGRAM_ADMIN_ID.

### 4. Caddy (secondbrain-caddy)

**Назначение:** Reverse proxy с автоматическим SSL.

**Особенности:**
- Автоматическое получение сертификатов Let's Encrypt
- HTTP/2 и HTTP/3 (QUIC)
- Gzip/Zstd сжатие
- Проксирование на внутренние сервисы
- Нормализация HTTP статус-кодов (201 → 200) для совместимости с Obsidian LiveSync

---

## 🔑 Учётные данные

### Production (.env на сервере)

```bash
# Домены
DOMAIN=notes.yakushev.me
API_DOMAIN=api.yakushev.me

# CouchDB
COUCHDB_USER=obsidian_admin
COUCHDB_PASSWORD=oLqAXQNR7shrTj8T/g5zsgWlYKS/uHeA8RXUD3UtnX0=
COUCHDB_DATABASE=obsidian_notes

# API
API_SECRET_KEY=9ab5aafdfeef5e7f4b48277456e4aa513a6f509e0c34d31f6eb2b2c0b772cf7b
NOTES_API_TOKEN=e5200c625d5b1649aa17a6303d84d9bf75123b3a4d78b1db

# Telegram
TELEGRAM_BOT_TOKEN=8186895415:AAGt2GO_vKNKWWKrh5b5el7gFycPx2kpMw4
TELEGRAM_ADMIN_ID=1525875
```

### SSH доступ

```bash
# Ключ
~/.ssh/id_ed25519_timeweb

# Подключение
ssh -i ~/.ssh/id_ed25519_timeweb root@82.147.71.198

# Или с паролем
ssh root@82.147.71.198
# Пароль: uMpGwL2Bm?+Q+g
```

### Timeweb CLI

```bash
# Конфигурация
~/.twc

# Проверка
twc account info
twc server list
```

---

## 💻 Локальная разработка

### Требования

- Docker + Docker Compose
- Python 3.11+
- Make

### Быстрый старт

```bash
# 1. Клонировать репозиторий
git clone <repo>
cd second-brain

# 2. Создать .env
cp .env.example .env
# Заполнить значения

# 3. Запустить
make dev

# 4. Проверить
curl http://localhost:8000/health
```

### Основные команды

```bash
make help           # Список всех команд
make dev            # Запуск в dev режиме
make stop           # Остановка
make logs           # Логи всех сервисов
make logs-api       # Логи API
make logs-db        # Логи CouchDB
make test           # Запуск тестов
make test-cov       # Тесты с покрытием
make lint           # Линтер
make format         # Форматирование
make clean          # Очистка
```

### Структура тестов

```bash
cd api
python -m pytest tests/ -v              # Все тесты
python -m pytest tests/test_api.py -v   # Только API тесты
python -m pytest --cov=. --cov-report=html  # С покрытием
```

---

## 🚀 Деплой и обновление

### Первый деплой (уже выполнен)

См. `docs/DEPLOY.md`

### Обновление кода

```bash
# 1. Локально: упаковать
make deploy-pack

# 2. Загрузить на сервер
make deploy-upload HOST=root@82.147.71.198

# 3. На сервере
ssh -i ~/.ssh/id_ed25519_timeweb root@82.147.71.198
cd ~/second-brain
tar -xzvf ~/second-brain-deploy.tar.gz --overwrite
docker compose up -d --build
```

### Быстрые команды

```bash
# Статус
make deploy-status HOST=root@82.147.71.198

# Логи
make deploy-logs HOST=root@82.147.71.198

# Перезапуск
make deploy-restart HOST=root@82.147.71.198

# SSH
make deploy-ssh HOST=root@82.147.71.198
```

---

## 📊 Мониторинг и отладка

### Проверка здоровья

```bash
# CouchDB
curl https://notes.yakushev.me/
# Ожидаемо: {"error":"unauthorized"...}

# API
curl https://api.yakushev.me/health
# Ожидаемо: {"status":"healthy","couchdb":{"connected":true,...}}
```

### Логи на сервере

```bash
ssh -i ~/.ssh/id_ed25519_timeweb root@82.147.71.198

# Все логи
cd ~/second-brain && docker compose logs -f

# Конкретный сервис
docker compose logs -f notes-api
docker compose logs -f couchdb
docker compose logs -f caddy
```

### Статус контейнеров

```bash
docker compose ps
docker stats
```

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| CouchDB unhealthy | Проверить пароль в health-check, перезапустить |
| API не отвечает | `docker compose restart notes-api` |
| SSL не работает | Проверить DNS, логи Caddy |
| Бот не отвечает | Проверить TELEGRAM_BOT_TOKEN, логи API |

---

## ⚠️ Известные особенности

### Исправленные проблемы

1. **httpx конфликт** — python-telegram-bot 21.0 требует httpx~=0.27, исправлено в requirements.txt
2. **CouchDB health-check** — требует авторизацию, добавлены credentials в docker-compose.yml
3. **CORS для Obsidian LiveSync** — изменён `origins` на `*` в local.ini, добавлены CORS заголовки в Caddyfile (исправлено 2026-01-12)
4. **HTTP 201 статус-код** — CouchDB возвращает 201 Created для PUT операций, но Obsidian LiveSync ожидает 200 OK. Добавлена нормализация статус-кодов в Caddy через handle_response (исправлено 2026-01-12)
5. **docker-compose version** — Warning о `version` устарел, можно убрать

### Текущие ограничения

1. **Один администратор** — бот отвечает только одному Telegram ID
2. **Нет E2E шифрования по умолчанию** — настраивается в Obsidian
3. **Нет бэкапов по расписанию** — только ручной `make backup`

---

## ✅ Что сделано

### Phase 1-6 (завершены)

- [x] Docker инфраструктура (CouchDB, Caddy, API)
- [x] REST API на FastAPI
- [x] Telegram бот
- [x] Web Viewer
- [x] Claude API интеграция для разработки
- [x] Unit тесты
- [x] Timeweb CLI интеграция
- [x] Production деплой

### Детали деплоя (2026-01-12)

- [x] SSH ключ добавлен на сервер
- [x] Docker установлен
- [x] UFW настроен
- [x] DNS настроен (notes.yakushev.me, api.yakushev.me)
- [x] Проект загружен
- [x] .env создан с credentials
- [x] Контейнеры запущены
- [x] База obsidian_notes создана
- [x] SSL сертификаты получены
- [x] Все сервисы healthy
- [x] CORS настроен для Obsidian LiveSync (исправлено позже в тот же день)

---

## 🔮 Что планируется

### Phase 8: Дополнительные фичи

**Высокий приоритет:**
- [ ] AI-обработка заметок (автотеги, саммари)
- [ ] Напоминания в Telegram
- [ ] Голосовые заметки (Whisper)

**Средний приоритет:**
- [ ] Экспорт в PDF
- [ ] Elasticsearch для поиска
- [ ] Граф связей

**Низкий приоритет:**
- [ ] Веб-редактор
- [ ] Публичные ссылки
- [ ] Мобильное приложение

---

## 📞 Контакты

- **Telegram:** @sam_yakushev (ID: 1525875)
- **Домен:** yakushev.me

---

*Документ актуален на 2026-01-12. См. STATUS_RULES.md для правил обновления.*
