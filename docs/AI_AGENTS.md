# AI Agents для Second Brain

## Обзор

Second Brain теперь оснащен интеллектуальной системой агентов, которая автоматически обрабатывает заметки, отвечает на вопросы и проводит глубокий анализ тем.

## Компоненты системы

### 1. Smart Note Taker Agent 📝

**Назначение:** Автоматически категоризирует и обогащает заметки метаданными.

**Возможности:**
- Автоматическое определение категории (папки)
- Извлечение тегов и ключевых слов
- Создание краткого резюме
- Определение приоритета и настроения
- Извлечение упоминаний людей, компаний, мест
- Генерация embedding для семантического поиска

**Пример использования:**
```
Просто отправьте текст в Telegram:
"Обсудили с командой новый подход к деплою ML моделей через Kubernetes"

Бот сохранит заметку с:
- Категорией: Projects/ML
- Тегами: [deployment, ml, kubernetes]
- Приоритетом: high
- Entities: упоминания команд, технологий
```

### 2. Q&A Agent ❓

**Назначение:** Отвечает на вопросы, используя вашу базу знаний.

**Технология:** 3-stage поиск
1. **Metadata filtering** — быстрая предфильтрация по тегам/категориям
2. **Hybrid search** — векторный + полнотекстовый поиск (top 50)
3. **Rerank + LLM** — Cohere reranking до top 10 + анализ Claude/OpenAI

**Пример использования:**
```
Вопрос: "Как мы деплоим модели в production?"

Бот:
1. Ищет релевантные заметки
2. Переранжирует по релевантности
3. Анализирует содержимое
4. Отвечает со ссылками на источники
```

### 3. Research Agent 🔍

**Назначение:** Глубокий анализ и синтез знаний по теме.

**Возможности:**
- Анализ до 100 заметок по теме
- Многопроходный анализ:
  - Pass 1: Обзор и ключевые темы
  - Pass 2: Связи и паттерны
  - Pass 3: Синтез и выводы
- Структурированный отчет

**Пример использования:**
```
1. Нажмите кнопку "🔍 Research Topic" в меню
2. Отправьте тему: "machine learning deployment"
3. Получите детальный отчет с:
   - Обзором всех найденных материалов
   - Анализом связей между заметками
   - Ключевыми инсайтами
   - Списком источников
```

## Настройка

### Переменные окружения

Добавьте в `.env`:

```bash
# LLM Providers
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
COHERE_API_KEY=...
DEFAULT_LLM_PROVIDER=claude

# Embeddings
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# Vector Database
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=second_brain_notes
```

### Запуск системы

```bash
# Запуск всех сервисов (включая Qdrant)
docker compose up -d

# Проверка статуса
curl http://localhost:8000/api/stats \
  -H "X-API-Token: YOUR_TOKEN"

# Ручной запуск индексации
curl -X POST http://localhost:8000/api/reindex \
  -H "X-API-Token: YOUR_TOKEN"
```

## Архитектура

```
Telegram User Message
        ↓
   Message Director (роутер)
        ↓
    ┌───┴────┬─────────┐
    ↓        ↓         ↓
Note Taker  Q&A   Research
    ↓        ↓         ↓
    └────────┴─────────┘
             ↓
    ┌────────┴─────────┐
    ↓                  ↓
 CouchDB            Qdrant
(заметки)        (vectors)
```

## API Endpoints

### Статистика системы

```bash
GET /api/stats
Authorization: X-API-Token: YOUR_TOKEN

Response:
{
  "couchdb": {
    "total_documents": 1234
  },
  "vector_store": {
    "total_vectors": 1100,
    "indexed_count": 1100
  },
  "agents": {
    "registered_agents": ["note_taker", "qa", "research"],
    "total_agents": 3
  },
  "indexing": {
    "pending_notes": 134
  }
}
```

### Ручная индексация

```bash
POST /api/reindex
Authorization: X-API-Token: YOUR_TOKEN

Response:
{
  "status": "started",
  "message": "Reindexing started in background"
}
```

## Метаданные в заметках

Каждая заметка автоматически обогащается метаданными в YAML frontmatter:

```yaml
---
created: 2026-01-13T15:30:00
source: telegram
from: sam_yakushev

# AI-generated metadata
ai_tags: [project, ai, deployment]
ai_category: Projects/AI
ai_summary: Discussion about deploying ML models to production
ai_keywords: [machine learning, docker, kubernetes]
ai_entities:
  people: [John Doe]
  companies: [OpenAI]
ai_sentiment: positive
ai_priority: high
ai_has_tasks: false
ai_indexed: 2026-01-13T15:30:15
---

Оригинальное содержимое заметки...
```

## Производительность

### Rate Limiting

- **Note Taker:** мгновенная обработка
- **Q&A Agent:** ~3-5 секунд (зависит от объема поиска)
- **Research Agent:** ~10-20 секунд (многопроходный анализ)
- **Background Indexer:** 5 заметок в минуту

### Стоимость API

Примерные затраты при умеренном использовании (100 заметок/день):
- **OpenAI Embeddings:** ~$0.01/день
- **Claude для metadata:** ~$0.50/день
- **Cohere Rerank:** ~$0.10/день
- **Итого:** ~$0.60/день или $18/месяц

## Troubleshooting

### Агенты не инициализируются

```bash
# Проверьте логи
docker compose logs notes-api | grep -i "agent\|llm"

# Возможные причины:
# 1. Не указаны API keys в .env
# 2. Qdrant не запущен
# 3. Неверные credentials
```

### Векторный поиск не работает

```bash
# Проверьте Qdrant
curl http://localhost:6333/collections

# Проверьте статус коллекции
curl http://localhost:6333/collections/second_brain_notes

# Перезапустите индексацию
curl -X POST http://localhost:8000/api/reindex \
  -H "X-API-Token: YOUR_TOKEN"
```

### Медленная обработка

1. Проверьте, что Qdrant запущен локально
2. Уменьшите `EMBEDDING_DIMENSIONS` (например, до 512)
3. Используйте более быструю модель embeddings
4. Ограничьте размер контекста для LLM

## Best Practices

### Оптимизация токенов

- Используйте `text-embedding-3-small` вместо `large`
- Ограничивайте длину заметок до 8000 символов
- Используйте Claude Haiku для простых задач

### Качество категоризации

- Поддерживайте четкую структуру папок в Obsidian
- Первые 20 папок используются для обучения
- Периодически проверяйте и корректируйте категории

### Семантический поиск

- Используйте развернутые вопросы
- Избегайте односложных запросов
- Уточняйте контекст в вопросах

## Roadmap

### Планируется добавить:

- [ ] Voice Handler — транскрибация голосовых сообщений (Whisper)
- [ ] Task Extractor — автоматическое извлечение задач
- [ ] Summarizer Agent — периодические digest'ы
- [ ] Multi-modal — поддержка изображений и PDF
- [ ] Feedback loop — улучшение на основе исправлений пользователя

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker compose logs notes-api -f`
2. Проверьте статус: `curl http://localhost:8000/api/stats`
3. Откройте issue в репозитории с логами

---

*Документация актуальна на 2026-01-13*
